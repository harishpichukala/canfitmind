import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Welcome to CanFitMind")
                .font(.largeTitle)
                .padding()
            Text("Track your fitness with AI")
                .font(.subheadline)
                .foregroundColor(.gray)
        }
    }
}
